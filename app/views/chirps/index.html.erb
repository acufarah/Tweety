<section class="section">
    <container>
       <div class="columns">
           <% if user_signed_in? %>
    		<%= render 'profile' %>
    		<%=render 'feed' %>
    	   <% else %>
    	   <div>Welcome to Tweety. Log In or Sign Up!</div>
           <% end %>
       </div>
    </container>
</section>
  